# ChipMate Fixture Design Note

The driver state machine resets on `DRIVER_EVENT_RESET` and accumulates scaled event values for other events.

This document is used by Windows offline Document RAG tests.
