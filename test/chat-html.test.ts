import { describe, expect, test } from "bun:test"
import { readFileSync } from "node:fs"
import { join } from "node:path"
import { createChatViewHtml, createNonce } from "../src/chat-html"

describe("chat webview html", () => {
  test("creates CSP-safe alphanumeric nonces", () => {
    for (let index = 0; index < 20; index++) {
      expect(createNonce()).toMatch(/^[A-Za-z0-9]{32}$/)
    }
  })

  test("uses the same nonce in CSP and script tag", () => {
    const html = createChatViewHtml("vscode-resource:", "AbC123")

    expect(html).toContain("script-src 'nonce-AbC123'")
    expect(html).toContain('<script nonce="AbC123">')
  })

  test("emits syntactically valid webview script", () => {
    const html = createChatViewHtml("vscode-resource:", "Syntax123")
    const scriptMatch = html.match(/<script[^>]*>([\s\S]*)<\/script>/)
    if (!scriptMatch) throw new Error("Expected generated webview HTML to contain a script tag.")

    expect(() => new Function("acquireVsCodeApi", scriptMatch[1])).not.toThrow()
  })

  test("contains the webview script smoke-test markers", () => {
    const html = createChatViewHtml("vscode-resource:", "Smoke123")

    expect(html).toContain("acquireVsCodeApi()")
    expect(html).toContain('class="topbar"')
    expect(html).toContain('class="app mode-connection-only history-closed history-narrow"')
    expect(html).toContain('id="historyToggle"')
    expect(html).toContain('id="settingsToggle"')
    expect(html).toContain('class="topbarIconSvg"')
    expect(html).toContain('viewBox="0 0 24 24"')
    expect(html).toContain('aria-hidden="true"')
    expect(html).toContain('<span class="srOnly">History</span>')
    expect(html).toContain('<span class="srOnly">New session</span>')
    expect(html).toContain('<span class="srOnly">Connection settings</span>')
    expect(html).toContain('<span class="srOnly">Refresh sessions</span>')
    expect(html).toContain('<span class="srOnly">Close history</span>')
    expect(html).not.toContain('id="historyToggle" class="icon" title="History" aria-label="History">History</button>')
    expect(html).not.toContain('id="newSession" class="icon" title="New session">+</button>')
    expect(html).not.toContain('id="refreshHistory" class="icon" title="Refresh sessions">Refresh</button>')
    expect(html).not.toContain('id="closeHistory" class="icon" title="Close history">x</button>')
    expect(html).not.toContain('<button id="settingsToggle" class="icon" title="Connection settings" aria-label="Connection settings">...</button>')
    expect(html).toContain('id="sessionList"')
    expect(html).toContain('id="messages"')
    expect(html).toContain("timelineItem")
    expect(html).toContain('class="composer"')
    expect(html).toContain('id="composerStatusBar"')
    expect(html).toContain('id="composerStatusToggle"')
    expect(html).not.toContain('id="panelStatusPill"')
    expect(html).toContain('id="contextStatusPill"')
    expect(html).toContain('id="indexStatusPill"')
    expect(html).toContain('id="guardStatusPill"')
    expect(html).toContain('id="usageStatusPill"')
    expect(html).toContain('id="composerStatusPopover"')
    expect(html).toContain('aria-controls="composerPanel"')
    expect(html).toContain('id="composerPanel"')
    expect(html).toContain("composerToggleIcon")
    expect(html).toContain("composerToggleFull")
    expect(html).toContain("composerToggleShort")
    expect(html).toContain("STATUS_ICONS")
    expect(html).toContain("statusIcon")
    expect(html).toContain("statusRingIcon")
    expect(html).toContain("statusBadge")
    expect(html).toContain("statusBadgeText")
    expect(html).toContain("bookOpen")
    expect(html).toContain("radar")
    expect(html).toContain("Hide input")
    expect(html).toContain("Show input")
    expect(html).toContain("composerStatusPill context")
    expect(html).toContain("composerStatusPill index")
    expect(html).toContain("composerStatusPill guard ok")
    expect(html).toContain("composerStatusPill usage pending")
    expect(html).toContain("composerWrap.collapsed")
    expect(html).toContain("let composerCollapsed = false")
    expect(html).toContain('let composerPinnedStatusPopover = ""')
    expect(html).toContain('let composerHoverStatusPopover = ""')
    expect(html).toContain("let composerHoverCloseTimer = 0")
    expect(html).toContain("toggleComposerPanel")
    expect(html).toContain("renderComposerStatusBar")
    expect(html).toContain("bindComposerStatusPill")
    expect(html).toContain("showComposerHoverPopover")
    expect(html).toContain("activeComposerStatusPopover")
    expect(html).toContain("toggleComposerStatusPopover")
    expect(html).toContain("renderComposerStatusPopover")
    expect(html).toContain("renderContextStatusPopover")
    expect(html).toContain("renderIndexStatusPopover")
    expect(html).toContain("renderGuardStatusPopover")
    expect(html).toContain("renderUsageStatusPopover")
    expect(html).toContain("closeComposerPopups")
    expect(html).toContain("panel.hidden = composerCollapsed")
    expect(html).toContain('toggle.setAttribute("aria-expanded"')
    expect(html).toContain("Show input and context panel")
    expect(html).toContain("Hide input and context panel")
    expect(html).toContain("Ctx ")
    expect(html).toContain("Guard warning")
    expect(html).toContain("Guard ok")
    expect(html).toContain("compactRing")
    expect(html).toContain("statusRing")
    expect(html).toContain("statusRing.indeterminate")
    expect(html).toContain("formatRingProgress")
    expect(html).toContain("indexStatusRing")
    expect(html).toContain("contextUsageRatio")
    expect(html).toContain("badgeText: count > 0")
    expect(html).toContain("ringIcon: STATUS_ICONS.radar")
    expect(html).toContain('bindComposerStatusPill("contextStatusPill", "context", true)')
    expect(html).toContain("color-mix(in srgb, currentColor")
    expect(html).toContain("transform: translateY(-1px)")
    expect(html).not.toContain('<span class="pillText">Panel</span>')
    expect(html).not.toContain('<span class="pillText">Ctx 0</span>')
    expect(html).not.toContain('<span class="pillText">Guard ok</span>')
    expect(html).not.toContain('<span class="pillText">Usage pending</span>')
    expect(html).toContain("statusPopoverActions")
    expect(html).toContain("contextRemoveButton")
    expect(html).toContain("modelMenuOpen = false")
    expect(html).toContain("agentMenuOpen = false")
    expect(html).toContain("mentionResults = []")
    expect(html).toContain('class="composerToolbar"')
    expect(html).toContain('id="composerHint"')
    expect(html).toContain('id="suggestions"')
    expect(html).toContain('id="send"')
    expect(html).toContain('el("send").textContent = state.sending ? "..." : "Send"')
    expect(html).toContain('class="composerActionRow"')
    expect(html).toContain('class="toggleShort">Current</span>')
    expect(html).toContain('class="toggleShort">Diag</span>')
    expect(html).toContain('id="exportMarkdown"')
    expect(html).toContain('class="secondary exportButton composerIconButton"')
    expect(html).toContain('aria-label="Export current chat to Markdown"')
    expect(html).toContain('class="buttonIcon"')
    expect(html).toContain('stroke-width="2.2"')
    expect(html).toContain('class="srOnly"')
    expect(html).toContain("border: 0;")
    expect(html).toContain("setIconButtonState")
    expect(html).toContain("Refreshing models")
    expect(html).not.toContain("composerIconActions")
    expect(html).toContain('type: "exportMarkdown", scope: "session"')
    expect(html).toContain('event.data.type === "exportStatus"')
    expect(html).toContain("looksLikeExportRequest")
    expect(html).toContain("localOnlyAgentBlocked() && !looksLikeExportRequest(text)")
    expect(html).not.toContain('id="guard"')
    expect(html).not.toContain('id="usageMeter"')
    expect(html).not.toContain('id="codeGraph"')
    expect(html).not.toContain('id="chips"')
    expect(html).toContain("Usage pending")
    expect(html).toContain("messageUsage")
    expect(html).toContain("historyError")
    expect(html).toContain('id="codeIntelligence"')
    expect(html).toContain("renderCodeIntelligence")
    expect(html).toContain("let codeIntelligenceVisible = false")
    expect(html).toContain('let selectedStateMachineId = ""')
    expect(html).toContain("openOrRefreshCodeIntelligence")
    expect(html).toContain("requestCodeIntelligenceRefresh")
    expect(html).toContain('state.loadingCodeIntelligence = true')
    expect(html).toContain('textContent = state.loadingCodeIntelligence ? "Loading..." : "Refresh"')
    expect(html).toContain('data-code-intel-hide')
    expect(html).toContain('data-code-intel-refresh')
    expect(html).toContain('hide.textContent = "Hide"')
    expect(html).toContain('inspect.textContent = "Inspect"')
    expect(html).toContain('jump.textContent = "Source"')
    expect(html).toContain("Open source evidence: ")
    expect(html).toContain("Loading local code intelligence")
    expect(html).toContain("No code intelligence snapshot is loaded yet")
    expect(html).toContain("State Machine Audit")
    expect(html).toContain("Transition Table")
    expect(html).toContain("unknown from-state")
    expect(html).toContain("low confidence")
    expect(html).toContain("candidate")
    expect(html).toContain("codeIntelTransitionTable")
    expect(html).toContain("Index States")
    expect(html).toContain("Code graph disabled")
    expect(html).toContain("Waiting for code graph indexing")
    expect(html).toContain("Indexed: ")
    expect(html).toContain("Indexing...")
    expect(html).toContain("Code RAG")
    expect(html).toContain('id="ragEmbeddingEndpoint"')
    expect(html).toContain('id="ragEmbeddingRequestDelayMs"')
    expect(html).toContain('id="ragEmbeddingMaxRequestsPerRun"')
    expect(html).toContain('id="ragEmbeddingMaxRetries"')
    expect(html).toContain('id="ragEmbeddingRetryBackoffMs"')
    expect(html).toContain('id="ragEmbeddingResumeAutomatically"')
    expect(html).toContain('id="ragEmbeddingResumeDelayMs"')
    expect(html).toContain('id="ragRerankEndpoint"')
    expect(html).toContain('id="saveRagSettings"')
    expect(html).toContain('id="testRagSettings"')
    expect(html).toContain('id="setRagApiKey"')
    expect(html).toContain('type: "saveRagSettings"')
    expect(html).toContain('type: "testRagSettings"')
    expect(html).toContain('type: "setRagApiKey"')
    expect(html).toContain('event.data.type === "ragStatus"')
    expect(html).toContain('renderCompletionStatus("Saving inline completion settings...", "info")')
    expect(html).toContain('renderRagStatus("Saving RAG settings...", "info")')
    expect(html).toContain(".detail.success")
    expect(html).toContain("Code graph degraded")
    expect(html).toContain("Code graph paused")
    expect(html).toContain("Code graph rescan scheduled")
    expect(html).toContain("Code graph error")
    expect(html).toContain('data-code-graph-action')
    expect(html).toContain('message: "indexCodeGraph"')
    expect(html).toContain('message: "rebuildCodeGraph"')
    expect(html).toContain('message: "pauseCodeGraph"')
    expect(html).toContain('message: "resumeCodeGraph"')
    expect(html).toContain('message: "cancelCodeGraph"')
    expect(html).toContain('message: "showCodeGraphStatus"')
    expect(html).toContain('message: "refreshCodeIntelligence"')
    expect(html).toContain('type: "openEvidence"')
    expect(html).toContain('id="modelSelect"')
    expect(html).toContain('id="modelTrigger"')
    expect(html).toContain('id="modelMenu"')
    expect(html).toContain('id="refreshModels"')
    expect(html).toContain('id="manualModel"')
    expect(html).toContain('el("attach").addEventListener("click"')
    expect(html).toContain('el("connect").addEventListener("click"')
    expect(html).toContain('el("test").addEventListener("click"')
    expect(html).toContain('let pendingAction = ""')
    expect(html).toContain('let settingsOpen = false')
    expect(html).toContain('let lastConnectionState = ""')
    expect(html).toContain('pendingAction = type === "testWithSettings" ? "test" : "connect"')
    expect(html).toContain("Test remote OpenCode connection without connecting")
    expect(html).toContain('type: "selectSession"')
    expect(html).toContain('type: "refreshSessions"')
    expect(html).toContain('type: "searchFilesForMention"')
    expect(html).toContain("onCodeGraphAction")
    expect(html).toContain("renderMarkdownInto")
    expect(html).toContain("messageActions")
    expect(html).toContain("copyAnswer")
    expect(html).toContain("copyMarkdown")
    expect(html).toContain("collapseMessage")
    expect(html).toContain("jumpStructure")
    expect(html).toContain("messageOutline")
    expect(html).toContain("messageOutlineButton")
    expect(html).toContain("messageLongHint")
    expect(html).toContain("longAnswer")
    expect(html).toContain("longExpanded")
    expect(html).toContain("scrollToNextMessageStructure")
    expect(html).toContain("messageStructureTargets")
    expect(html).toContain("markdownToPlainText")
    expect(html).toContain('aria-label", "Answer outline"')
    expect(html).toContain('aria-expanded", options.isCollapsed ? "false" : "true"')
    expect(html).toContain("detectTableBlock")
    expect(html).toContain("detectMarkdownTable")
    expect(html).toContain("detectDelimitedTable")
    expect(html).toContain("detectPlainAlignedTable")
    expect(html).toContain("detectKeyValueBlock")
    expect(html).toContain("looksLikeMarkdownListRows")
    expect(html).toContain("isMarkdownListMarkerCell")
    expect(html).toContain("looksLikeMarkdownLabelRows")
    expect(html).toContain("isMarkdownLabelCellSafe")
    expect(html).toContain('const unordered = line.match(/^[-*+]')
    expect(html).toContain("tableBlock")
    expect(html).toContain("tableToolbar")
    expect(html).toContain("tableScroll")
    expect(html).toContain("tableRaw")
    expect(html).toContain("copyTableMarkdown")
    expect(html).toContain("copyTableCsv")
    expect(html).toContain("toggleTableRaw")
    expect(html).toContain("tableToMarkdown")
    expect(html).toContain("tableToCsv")
    expect(html).toContain("Raw table text")
    expect(html).toContain('block.setAttribute("role", "region")')
    expect(html).toContain('block.setAttribute("data-table-kind"')
    expect(html).toContain('raw.setAttribute("aria-pressed"')
    expect(html).toContain("onPopupMenuKeydown")
    expect(html).toContain("focusPopupMenuItem")
    expect(html).toContain('event.key === "ArrowDown"')
    expect(html).toContain('event.key === "ArrowUp"')
    expect(html).toContain('key === "Home"')
    expect(html).toContain('key === "End"')
    expect(html).toContain('event.key !== "Escape"')
    expect(html).toContain("forced-colors: active")
    expect(html).toContain("--chat-user-font-size")
    expect(html).toContain(".timelineItem.assistant .messageBody")
    expect(html).toContain("mdQuote")
    expect(html).toContain("mdDivider")
    expect(html).toContain('const tag = ordered ? "ol" : "ul"')
    expect(html).toContain("document.createElement(tag)")
    expect(html).toContain("mdStrong")
    expect(html).toContain("mdEm")
    expect(html).toContain("nextInlineMarker")
    expect(html).toContain("findSingleStar")
    expect(html).toContain("codeLanguage")
    expect(html).toContain("appendHighlightedCode")
    expect(html).toContain("tokenizeCode")
    expect(html).toContain("normalizeCodeLanguage")
    expect(html).toContain("syntaxKeyword")
    expect(html).toContain("syntaxString")
    expect(html).toContain("syntaxComment")
    expect(html).toContain("syntaxFunction")
    expect(html).toContain("syntaxInserted")
    expect(html).toContain('copy.title = "Copy code block"')
    expect(html).toContain('button.textContent = "Copied"')
    expect(html).toContain("copyCode")
    expect(html).toContain("thinking")
    expect(html).toContain("prefers-reduced-motion")
    expect(html).toContain("history-closed history-narrow")
    expect(html).toContain("history-wide")
    expect(html).toContain("flex: 0 0 auto")
    expect(html).toContain("No current file captured")
    expect(html).toContain("autoContext")
    expect(html).toContain("Current: ")
    expect(html).toContain("mentionedFiles")
    expect(html).toContain("Local-only guard active")
    expect(html).toContain("guardStatusDetail")
    expect(html).toContain("VS Code agent: ")
    expect(html).toContain('id="agentTrigger"')
    expect(html).toContain('id="agentMenu"')
    expect(html).toContain("localOnlyAgentBlocked")
    expect(html).toContain("agentMatchesRequired")
    expect(html).toContain("canonicalAgentName")
    expect(html).toContain("Required VS Code local agent is unavailable")
    expect(html).toContain("Unavailable for VS Code local mode")
    expect(html).toContain("button.disabled = Boolean(disabled)")
    expect(html).toContain(".modelMenuItem.disabled")
    expect(html).toContain("Model: server default")
    expect(html).toContain("requestId")
    expect(html).toContain("Searching workspace files")
    expect(html).toContain("No files found")
    expect(html).toContain("Index truncated")
    expect(html).toContain("server filesystem tool used")
    expect(html).toContain('vscode.postMessage({ type: "ready" })')
    expect(html).toContain("OpenCode Remote UI loading")
  })

  test("uses mutually exclusive connection, chat, and settings modes", () => {
    const html = createChatViewHtml("vscode-resource:", "Modes123")

    expect(html).toContain("mode-connection-only")
    expect(html).toContain("mode-settings-page")
    expect(html).toContain('id="completionSettingsGroup"')
    expect(html).toContain('id="completionProfile"')
    expect(html).toContain('value="qwen-coder-fim"')
    expect(html).toContain('profile: el("completionProfile").value')
    expect(html).toContain('el("completionProfile").value = completion.profile || "generic-chat";')
    expect(html).toContain(".app.mode-connection-only .completionSettingsGroup,")
    expect(html).toContain(".app.mode-connection-only .ragSettingsGroup { display: none; }")
    expect(html).toContain(".app.mode-connection-only .body")
    expect(html).toContain(".app.mode-settings-page .body { display: none; }")
    expect(html).toContain('el("app").className = "app mode-" + mode')
    expect(html).toContain("function currentViewMode()")
    expect(html).toContain('return (state.connectionState || "disconnected") === "connected" ? "chat" : "connection-only";')
    expect(html).toContain('if (nextConnectionState === "connected" && (lastConnectionState !== "connected" || wasPendingConnect)) settingsOpen = false;')
    expect(html).not.toContain('settingsOpen || state.connectionState !== "connected"')
  })

  test("does not auto-refresh when the webview resolves", () => {
    const source = readFileSync(join(import.meta.dir, "..", "src", "chat-view.ts"), "utf8")
    const start = source.indexOf("resolveWebviewView")
    const end = source.indexOf("setConnectionState", start)
    const resolveBody = source.slice(start, end)

    expect(resolveBody).not.toContain("refresh()")
    expect(resolveBody).toContain("postState()")
  })

  test("checks markdown lists before aligned table detection", () => {
    const html = createChatViewHtml("vscode-resource:", "Order123")
    const start = html.indexOf("function renderTextMarkdown")
    const end = html.indexOf("function detectTableBlock", start)
    const renderText = html.slice(start, end)

    expect(renderText.indexOf("const unordered")).toBeGreaterThan(-1)
    expect(renderText.indexOf("const ordered")).toBeGreaterThan(-1)
    expect(renderText.indexOf("const table = detectTableBlock(lines, index)")).toBeGreaterThan(-1)
    expect(renderText.indexOf("const unordered")).toBeLessThan(renderText.indexOf("const table = detectTableBlock(lines, index)"))
    expect(html).toContain("looksLikeMarkdownListRows(rows) || looksLikeMarkdownLabelRows(rows)")
    expect(html).toContain("isMarkdownListMarkerCell(row[0])")
    expect(html).toContain("String.fromCharCode(0xff1a)")
  })

  test("chat provider handles code graph webview actions", () => {
    const source = readFileSync(join(import.meta.dir, "..", "src", "chat-view.ts"), "utf8")

    expect(source).toContain('{ type: "indexCodeGraph" }')
    expect(source).toContain('{ type: "rebuildCodeGraph" }')
    expect(source).toContain('{ type: "pauseCodeGraph" }')
    expect(source).toContain('{ type: "resumeCodeGraph" }')
    expect(source).toContain('{ type: "cancelCodeGraph" }')
    expect(source).toContain('{ type: "showCodeGraphStatus" }')
    expect(source).toContain('{ type: "refreshCodeIntelligence" }')
    expect(source).toContain('{ type: "openEvidence"; path: string; line?: number }')
    expect(source).toContain('{ type: "exportMarkdown"; scope?: ExportScope; filenameHint?: string }')
    expect(source).toContain('case "exportMarkdown"')
    expect(source).toContain("showSaveDialog")
    expect(source).toContain("workspace.fs.writeFile")
    expect(source).toContain("parseExplicitExportCommand")
    expect(source).toContain("isExportIntentCandidate")
    expect(source).toContain("parseExportIntentResponse")
    expect(source).toContain("hiddenExportIntentSessions")
    expect(source).toContain('case "indexCodeGraph"')
    expect(source).toContain('case "rebuildCodeGraph"')
    expect(source).toContain('case "pauseCodeGraph"')
    expect(source).toContain('case "resumeCodeGraph"')
    expect(source).toContain('case "cancelCodeGraph"')
    expect(source).toContain('case "showCodeGraphStatus"')
    expect(source).toContain('case "refreshCodeIntelligence"')
    expect(source).toContain('case "openEvidence"')
    expect(source).toContain("indexWorkspace(force)")
    expect(source).toContain("showStatus()")
    expect(source).toContain("intelligenceSnapshot()")
    expect(source).toContain("if (this.loadingCodeIntelligence)")
    expect(source).toContain("private codeIntelligenceError")
    expect(source).toContain("codeIntelligenceError: this.codeIntelligenceError")
  })
})
