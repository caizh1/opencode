import { cFixture, doc, snippet } from "./_helpers"

export default [
  cFixture({
    id: "B01-pointer-member",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { hal_status_t status; uint32_t count; } driver_ctx_t;\nhal_status_t get_status(driver_ctx_t *ctx)\n{\n    return ctx-><|cursor|>;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a pointer member access",
    mustContain: ["return ctx->status;"],
    maxLines: 1,
    modelOutputs: ["status"],
  }),
  cFixture({
    id: "B02-struct-dot-member",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint32_t baudrate; uint8_t parity; } uart_config_t;\nuint32_t get_baudrate(uart_config_t cfg)\n{\n    return cfg.<|cursor|>;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a struct dot member access",
    mustContain: ["return cfg.baudrate;"],
    maxLines: 1,
    modelOutputs: ["baudrate"],
  }),
  cFixture({
    id: "B03-null-guard",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint32_t value; } sensor_t;\nhal_status_t sensor_read(sensor_t *sensor, uint32_t *out)\n{\n    if (<|cursor|>) {\n        return HAL_ERR;\n    }\n    *out = sensor->value;\n    return HAL_OK;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a pointer null guard",
    mustContain: ["if (sensor == NULL || out == NULL)"],
    maxLines: 1,
    modelOutputs: ["sensor == NULL || out == NULL"],
  }),
  cFixture({
    id: "B04-typedef-alias",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint16_t raw; } adc_sample_t;\nadc_sample_t make_sample(uint16_t raw)\n{\n    adc_<|cursor|>\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a typedef alias declaration",
    mustContain: ["adc_sample_t sample = { .raw = raw };"],
    maxLines: 2,
    modelOutputs: ["sample_t sample = { .raw = raw };\n    return sample;"],
  }),
  cFixture({
    id: "B05-const-pointer",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint32_t id; } device_t;\nuint32_t device_id(const device_t *dev)\n{\n    const device_t *local = <|cursor|>\n    return local->id;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a const pointer assignment",
    mustContain: ["const device_t *local = dev;"],
    maxLines: 1,
    modelOutputs: ["dev;"],
  }),
  cFixture({
    id: "B06-function-pointer",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef hal_status_t (*init_fn_t)(void);\nstatic hal_status_t default_init(void) { return HAL_OK; }\nhal_status_t call_init(void)\n{\n    init_fn_t init = <|cursor|>\n    return init();\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a function pointer initializer",
    mustContain: ["init_fn_t init = default_init;"],
    maxLines: 1,
    modelOutputs: ["default_init;"],
  }),
  cFixture({
    id: "B07-nested-struct",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint32_t error_count; } stats_t;\ntypedef struct { stats_t stats; } driver_t;\nuint32_t errors(const driver_t *drv)\n{\n    return drv-><|cursor|>;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete nested struct member access",
    mustContain: ["return drv->stats.error_count;"],
    maxLines: 1,
    modelOutputs: ["stats.error_count"],
  }),
  cFixture({
    id: "B08-array-member",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint8_t slots[4]; } table_t;\nuint8_t first_slot(const table_t *table)\n{\n    return table-><|cursor|>;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete an array member read",
    mustContain: ["return table->slots[0];"],
    maxLines: 1,
    modelOutputs: ["slots[0]"],
  }),
  cFixture({
    id: "B09-opaque-handle",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct driver_impl driver_handle_t;\nstruct driver_impl { bool ready; };\nbool handle_ready(driver_handle_t *handle)\n{\n    if (handle == NULL) {\n        return false;\n    }\n    return <|cursor|>\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete an opaque handle readiness expression",
    mustContain: ["return handle->ready;"],
    maxLines: 1,
    modelOutputs: ["handle->ready;"],
  }),
  cFixture({
    id: "B10-selected-info-bad-contract",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/context.c",
    document: doc("typedef struct { uint32_t status; uint32_t state; } driver_ctx_t;\nhal_status_t update_state(driver_ctx_t *ctx)\n{\n    ctx->st<|cursor|> = 1u;\n    return HAL_OK;\n}\n"),
    selectedCompletionInfo: {
      range: { startLine: 22, startCharacter: 9, endLine: 22, endCharacter: 11 },
      text: "status",
    },
    triggerKind: "automatic",
    expectedIntent: "surface a bad selectedCompletionInfo edit contract",
    mustContain: ["ctx->status = 1u;"],
    maxLines: 1,
    modelOutputs: ["state"],
  }),
  cFixture({
    id: "B11-member-access-type-context",
    category: "B. Pointers structs and typedefs",
    path: "src/driver/queue.c",
    document: doc("typedef struct { uint32_t depth; bool ready; } queue_state_t;\nbool queue_ready(queue_state_t *state)\n{\n    return state-><|cursor|>;\n}\n"),
    retrievedSnippets: [
      snippet({
        kind: "type",
        name: "queue_state_t",
        path: "include/driver/queue.h",
        text: "typedef struct { uint32_t depth; bool ready; } queue_state_t;",
      }),
    ],
    triggerKind: "automatic",
    expectedIntent: "complete a pointer member using visible type context",
    mustContain: ["return state->ready;"],
    contextMustContain: ["queue_state_t"],
    maxLines: 1,
    modelOutputs: ["ready;\nif (state == NULL) {\n        return false;\n    }"],
  }),
]
