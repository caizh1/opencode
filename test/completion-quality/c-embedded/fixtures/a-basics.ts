import { cFixture, doc, fn } from "./_helpers"

export default [
  cFixture({
    id: "A01-return-expression",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: fn("read_flags", "    const uint32_t flags = BIT(2);\n    return <|cursor|>\n", "uint32_t"),
    triggerKind: "automatic",
    expectedIntent: "complete a simple return expression",
    mustContain: ["return flags;"],
    maxLines: 1,
    modelOutputs: ["flags;"],
  }),
  cFixture({
    id: "A02-if-condition",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: fn("feature_enabled", "    uint32_t flags = BIT(0);\n    if (<|cursor|>) {\n        return HAL_OK;\n    }\n    return HAL_ERR;\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a local if condition",
    mustContain: ["if (flags & BIT(0))"],
    maxLines: 1,
    modelOutputs: ["flags & BIT(0)"],
  }),
  cFixture({
    id: "A03-for-loop-header",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: doc("hal_status_t sum_samples(const uint16_t *samples, size_t count)\n{\n    uint32_t sum = 0u;\n    for (<|cursor|>) {\n        sum += samples[i];\n    }\n    return (hal_status_t)sum;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a bounded for-loop header",
    mustContain: ["for (size_t i = 0; i < count; i++)"],
    maxLines: 1,
    modelOutputs: ["size_t i = 0; i < count; i++"],
  }),
  cFixture({
    id: "A04-switch-case",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: fn("map_state", "    uint32_t state = 1u;\n    switch (state) {\n    <|cursor|>\n    }\n"),
    triggerKind: "manual",
    expectedIntent: "complete a compact switch body",
    mustContain: ["case 1u:", "default:"],
    maxLines: 4,
    modelOutputs: ["case 1u:\n        return HAL_OK;\n    default:\n        return HAL_ERR;"],
  }),
  cFixture({
    id: "A05-designated-initializer",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: doc("typedef struct { uint8_t id; bool enabled; } channel_cfg_t;\nchannel_cfg_t default_channel(void)\n{\n    channel_cfg_t cfg = <|cursor|>\n    return cfg;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a C designated initializer",
    mustContain: ["channel_cfg_t cfg = { .id = 1u, .enabled = true };"],
    maxLines: 1,
    modelOutputs: ["{ .id = 1u, .enabled = true };"],
  }),
  cFixture({
    id: "A06-sizeof-array-count",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: fn("sample_count", "    uint16_t samples[8];\n    return <|cursor|>\n", "size_t"),
    triggerKind: "automatic",
    expectedIntent: "complete an ARRAY_SIZE-style expression",
    mustContain: ["return ARRAY_SIZE(samples);"],
    mustContainAny: [["ARRAY_SIZE(samples)", "sizeof(samples) / sizeof(samples[0])"]],
    maxLines: 1,
    modelOutputs: ["ARRAY_SIZE(samples);"],
  }),
  cFixture({
    id: "A07-enum-value",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: doc("typedef enum { MODE_IDLE, MODE_RUN } app_mode_t;\napp_mode_t next_mode(bool armed)\n{\n    return armed ? <|cursor|> : MODE_IDLE;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete an enum value expression",
    mustContain: ["return armed ? MODE_RUN : MODE_IDLE;"],
    maxLines: 1,
    modelOutputs: ["MODE_RUN"],
  }),
  cFixture({
    id: "A08-width-cast",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: fn("low_word", "    uint32_t raw = 0x12345678u;\n    return <|cursor|>\n", "uint16_t"),
    triggerKind: "automatic",
    expectedIntent: "complete a width-safe cast",
    mustContain: ["return (uint16_t)(raw & 0xffffu);"],
    maxLines: 1,
    modelOutputs: ["(uint16_t)(raw & 0xffffu);"],
  }),
  cFixture({
    id: "A09-local-variable",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: fn("limit_timeout", "    uint32_t lim<|cursor|>\n    return (hal_status_t)limit;\n"),
    triggerKind: "automatic",
    expectedIntent: "finish a local variable declaration without duplicating the stem",
    mustContain: ["uint32_t limit = 1000u;"],
    maxLines: 1,
    modelOutputs: ["limit = 1000u;"],
  }),
  cFixture({
    id: "A10-suffix-stability",
    category: "A. C basics and local expressions",
    path: "src/app/basic.c",
    document: doc("static uint32_t next_sample(void) { return 1u; }\nstatic uint32_t read_sample(void) { return 2u; }\nuint32_t call_sample(void)\n{\n    return <|cursor|>;\n}\n"),
    triggerKind: "automatic",
    expectedIntent: "complete a middle-of-line expression and expose repeat instability",
    mustContain: ["return next_sample();"],
    maxLines: 1,
    modelOutputs: ["next_sample()", "read_sample()"],
  }),
]
