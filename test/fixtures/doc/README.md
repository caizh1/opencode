# DOC Fixture

`test01.doc` is copied from the `word-extractor` upstream test data:

https://github.com/morungos/node-word-extractor/blob/develop/__tests__/data/test01.doc

The upstream project is MIT licensed. The fixture is used only by local tests to
verify legacy Word `.doc` extraction without relying on Word, `textutil`, or
external converter binaries at test time.
